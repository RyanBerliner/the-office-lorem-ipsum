# The Office Lorem Ipsum

To use `npm i && npm run build`, then open index.html in your browser.
To dev `npm i && npm start`, then open index.html in your browser (no hot reloading, will need to refresh manually).